import React from 'react';
import { Icon } from '../../shared/components';

const CreateIcon = ({ ...prp }: any) => {
    return <Icon name="plus-square" color='white' {...prp} />;
}

export default CreateIcon