import React from 'react';
import { Icon } from '../../shared/components';

const CommentIcon = ({ ...prp }: any) => {
    return <Icon name="message-square" {...prp} />;
}

export default CommentIcon